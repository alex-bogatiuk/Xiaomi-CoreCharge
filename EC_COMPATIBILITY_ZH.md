# 通过 RWeverything 验证 EC 寄存器兼容性指南

本指南将帮助您验证您的小米/Redmi笔记本电脑的嵌入式控制器 (EC) 寄存器地址及数值是否与 **Xiaomi CoreCharge** 的默认设置完全兼容。

通过使用 **RWeverything** 实时查看笔记本电脑的 EC 内存，并与官方 **小米电脑管家** 的设置进行对比，您可以确认硬件的兼容性。

---

## 第一步：下载并运行 RWeverything
1. 从官方网站下载 **RWeverything**（推荐下载绿色版 *Portable*）：[rweverything.com](http://rweverything.com/download/)。
2. 解压压缩包，**务必以管理员身份运行** `RW.exe`（64位系统为 `RW64.exe`）（右键点击 -> *以管理员身份运行*）。这是获取低级别硬件访问权限的必要步骤。

---

## 第二步：打开嵌入式控制器 (EC) 面板
1. 在顶部工具栏中，点击 **EC** 按钮（带 "EC" 字样的绿色芯片图标），或在顶部菜单选择：**Access** -> **Embedded Controller**。
2. 随后将打开一个 16x16 的表格，展示 256 字节 of EC 内存（地址从 `0x00` 到 `0xFF`）：
   * **行**（纵向）标记为 `00`, `10`, `20` ... `A0` ... `F0`（代表地址的第一位十六进制数）。
   * **列**（横向）标记为 `0` 到 `F`（代表地址的第二位十六进制数）。

---

## 第三步：验证电池养护充电地址 (`0xA4` 和 `0xA7`)

我们需要验证以下两个地址：
* **`0xA4`**（行 **A0**，列 **4**）—— 控制养护充电功能的开启和关闭。
* **`0xA7`**（行 **A0**，列 **7**）—— 存储充电限制百分比。

### 测试步骤：
1. 将 RWeverything 窗口与官方 **小米电脑管家**（电池设置页面）并排摆放。
2. 在 RWeverything 表格中找到行 **A0**、列 **7**（即地址 `0xA7`）的单元格。
3. 在**小米电脑管家**中修改充电限制：
   * 设置为 **80%**：单元格 **A7** 的数值应变为 **`50`**（十六进制下的 80）。
   * 设置为 **60%**：单元格 **A7** 的数值应变为 **`3C`**（十六进制下的 60）。
   * 设置为 **100%**（或关闭限制）：单元格 **A7** 的数值应变为 **`64`**（十六进制下的 100）。
4. 随后在表格中找到行 **A0**、列 **4**（即地址 `0xA4`）的单元格：
   * 在官方电脑管家中**开启**电池养护充电：单元格 **A4** 的数值应发生变化，且其最低位二进制数变为 `1`（通常整个单元格变为 `01` 或其他奇数，例如 `21`，其二进制为 `00100001`）。
   * **关闭**电池养护充电：单元格 **A4** 的数值最低位二进制数应变为 `0`（通常整个单元格变为 `20` 或 `00` 等任意偶数）。

> **结果：** 如果单元格 **A4** 和 **A7** 的数值变化符合上述描述，说明您的笔记本电脑与默认的电池养护充电设置**完全兼容**。

---

## 第四步：验证性能模式切换地址 (`0x68`)

我们需要验证以下地址：
* **`0x68`**（行 **60**，列 **8**）—— 存储当前活跃的性能模式。

### 测试步骤：
1. 在 RWeverything 表格中找到行 **60**、列 **8**（即地址 `0x68`）的单元格。
2. 在官方**小米电脑管家**中切换性能模式（或使用 `Fn + K` 快捷键），并观察单元格 **68** 的变化：
   * 切换至 **省电模式** (Eco / Энергосберегающий)：单元格 **68** 的数值应变为 **`0A`**。
   * 切换至 **智能模式** (Balanced / Сбалансированный)：单元格数值应变为 **`09`**。
   * 切换至 **极速模式** (Performance / Производительный)：单元格数值应变为 **`03`**。

*(注意：如果您的机型支持狂暴/静谧模式，它们将分别显示为 `04` 和 `02`。)*

> **结果：** 如果切换性能模式时单元格 **68** 准确变为上述数值，说明您的笔记本性能模式管理与该程序**完全兼容**。

---

## 验证结论
* **所有数值完全吻合：** 您的笔记本电脑 100% 兼容。您可以放心卸载臃肿的小米电脑管家，使用这款轻量化的替代工具！
* **数值会发生变化，但单元格地址不同：** 您的笔记本支持 EC 控制，但使用了不同的地址偏移。您可以在 [main_clean.cpp](file:///c:/Users/user/go/src/xiaomi_corecharge/xiaomi_corecharge/main_clean.cpp) 中修改对应的 EC 地址宏定义，并重新编译项目。
* **数值完全没有任何变化：** 您的笔记本可能使用了其他接口（例如 WMI）而不是直接通过 EC 寄存器控制。此程序目前不支持该设备。
